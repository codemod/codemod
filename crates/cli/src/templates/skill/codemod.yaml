schema_version: "1.0"

name: "{name}"
version: "0.1.0"
description: "{description}"
author: "{author}"
license: "{license}"
{repository}

targets:
  languages: ["{language}"]

keywords: ["transformation", "migration", "skill"]

registry:
  access: "{access}"
  visibility: "{visibility}"

workflow: "workflow.yaml"

capabilities: []
