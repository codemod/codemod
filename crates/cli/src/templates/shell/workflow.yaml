# yaml-language-server: $schema=https://raw.githubusercontent.com/codemod/codemod/refs/heads/main/schemas/workflow.json

version: "1"
nodes:
  - id: setup
    name: Setup Environment
    type: automatic
    steps:
      - id: init
        name: Initialize
        run: $CODEMOD_PATH/scripts/setup.sh

  - id: transform
    name: Apply Transformations
    type: automatic
    depends_on: [setup]
    steps:
      - id: apply
        name: Apply Changes
        run: $CODEMOD_PATH/scripts/transform.sh

  - id: cleanup
    name: Cleanup
    type: automatic
    depends_on: [transform]
    steps:
      - id: clean
        name: Clean temporary files
        run: $CODEMOD_PATH/scripts/cleanup.sh
