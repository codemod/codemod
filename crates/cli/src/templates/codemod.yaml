schema_version: "1.0"

name: "{name}"
version: "0.1.0"
description: "{description}"
author: "{author}"
license: "{license}"
{repository}

targets:
  languages: ["{language}"]

keywords: ["transformation", "migration"]

registry:
  access: "{access}"
  visibility: "{visibility}"

capabilities: []

workflows:
  - name: main
    default: true
    path: workflow.yaml
