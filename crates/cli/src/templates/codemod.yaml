schema_version: "1.0"

name: "{name}"
version: "0.1.0"
description: "{description}"
author: "{author}"
license: "{license}"
workflow: "workflow.yaml"
{repository}

targets:
  languages: ["{language}"]

keywords: ["transformation", "migration"]

registry:
  access: "{access}"
  visibility: "{visibility}"

capabilities: []
